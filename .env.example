# MongoDB 連線字串（必需）
# 格式: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/
MONGO_URI=

# 管理員密碼（選填，用於管理後台和清空所有檔案功能）
# 建議使用強密碼，例如：openssl rand -hex 32
ADMIN_SECRET=

# Email 通知設定（選填，用於壓縮完成後寄送通知信）
# Gmail 使用者需要啟用「應用程式密碼」: https://myaccount.google.com/apppasswords
MAIL_USERNAME=
MAIL_PASSWORD=

# 並發任務數量限制（選填，預設為 3）
# 根據伺服器效能調整，建議值: 2-5
MAX_CONCURRENT_TASKS=3

# 檔案大小限制（選填，預設為 100MB）
# 單位: MB，根據伺服器記憶體和頻寬調整
MAX_FILE_SIZE_MB=100

# 部署平台說明：
# - 本地開發：複製此檔案為 .env 並填入實際值
# - Zeabur：在環境變數設定中直接設定上述變數
# - Heroku：使用 heroku config:set MONGO_URI=your_value
# - Docker：在 docker-compose.yml 中設定環境變數
